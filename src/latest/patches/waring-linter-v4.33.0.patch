diff --git a/Warning/Waring/Analytic/ChenFiveGlobalization.lean b/Warning/Waring/Analytic/ChenFiveGlobalization.lean
--- a/Warning/Waring/Analytic/ChenFiveGlobalization.lean
+++ b/Warning/Waring/Analytic/ChenFiveGlobalization.lean
@@ -39,8 +39,8 @@
 theorem primitiveFiveCoefficients_iff_modPrime {p : Nat} (hp : p.Prime)
     (a : FiveCoefficients p) :
     PrimitiveFiveCoefficients a ↔ PrimitiveFiveCoefficientsModPrime a := by
-  letI : Fact p.Prime := ⟨hp⟩
-  letI : NeZero p := ⟨hp.ne_zero⟩
+  let : Fact p.Prime := ⟨hp⟩
+  let : NeZero p := ⟨hp.ne_zero⟩
   constructor
   · rintro ⟨u, hu⟩
     by_contra ha
@@ -228,8 +228,8 @@
     · intro m n hmOne hnOne hcoprime hmInd hnInd hmn b hb
       have hm : m ≠ 0 := by omega
       have hn : n ≠ 0 := by omega
-      letI : NeZero m := ⟨hm⟩
-      letI : NeZero n := ⟨hn⟩
+      let : NeZero m := ⟨hm⟩
+      let : NeZero n := ⟨hn⟩
       have hprimitive :=
         primitiveFiveCoefficients_normalizedCrt hcoprime hb
       have hLeft := hmInd hm (normalizedCrtFiveLeft hcoprime b) hprimitive.1
diff --git a/Warning/Waring/Analytic/ChenFiveLocal.lean b/Warning/Waring/Analytic/ChenFiveLocal.lean
--- a/Warning/Waring/Analytic/ChenFiveLocal.lean
+++ b/Warning/Waring/Analytic/ChenFiveLocal.lean
@@ -58,7 +58,7 @@
     ((a 0).val : ZMod p) ≠ 0 ∨ ((a 1).val : ZMod p) ≠ 0 ∨
       ((a 2).val : ZMod p) ≠ 0 ∨ ((a 3).val : ZMod p) ≠ 0 ∨
       ((a 4).val : ZMod p) ≠ 0 := by
-  letI : NeZero p := ⟨hp.ne_zero⟩
+  let : NeZero p := ⟨hp.ne_zero⟩
   rcases exists_primePowerFiveReduction_ne_zero p alpha hp hAlpha a ha with
     ⟨i, hi⟩
   simp only [primePowerFiveReduction, ZMod.castHom_apply,
@@ -86,9 +86,9 @@
     ‖@fivePolynomialCompleteSum (p ^ alpha)
         ⟨pow_ne_zero alpha hp.ne_zero⟩ a‖ ≤
       K * (((p ^ alpha : Nat) : Real) ^ (4 / 5 : Real)) := by
-  letI : Fact p.Prime := ⟨hp⟩
-  letI : NeZero p := ⟨hp.ne_zero⟩
-  letI : NeZero (p ^ alpha) := ⟨pow_ne_zero alpha hp.ne_zero⟩
+  let : Fact p.Prime := ⟨hp⟩
+  let : NeZero p := ⟨hp.ne_zero⟩
+  let : NeZero (p ^ alpha) := ⟨pow_ne_zero alpha hp.ne_zero⟩
   rw [fivePolynomialCompleteSum_eq_integerRepresentatives]
   apply chenFour_primePower_rpow_bound_of_primeField
     p alpha hpLarge K hK hprime hAlpha
diff --git a/Warning/Waring/Analytic/ChenFourDerivative.lean b/Warning/Waring/Analytic/ChenFourDerivative.lean
--- a/Warning/Waring/Analytic/ChenFourDerivative.lean
+++ b/Warning/Waring/Analytic/ChenFourDerivative.lean
@@ -72,7 +72,7 @@
     (hp : 11 ≤ p) (a₀ a₁ a₂ a₃ a₄ : ZMod p)
     (hcoeff : a₀ ≠ 0 ∨ a₁ ≠ 0 ∨ a₂ ≠ 0 ∨ a₃ ≠ 0 ∨ a₄ ≠ 0) :
     fifthPolynomialDerivative a₀ a₁ a₂ a₃ a₄ ≠ 0 := by
-  letI : Fact p.Prime := ⟨hpPrime⟩
+  let : Fact p.Prime := ⟨hpPrime⟩
   have hcast (n : Nat) (hnPos : 0 < n) (hn : n < p) :
       (n : ZMod p) ≠ 0 := by
     rw [Ne, ZMod.natCast_eq_zero_iff]
diff --git a/Warning/Waring/Analytic/ChenTenCumulativeVolume.lean b/Warning/Waring/Analytic/ChenTenCumulativeVolume.lean
--- a/Warning/Waring/Analytic/ChenTenCumulativeVolume.lean
+++ b/Warning/Waring/Analytic/ChenTenCumulativeVolume.lean
@@ -82,8 +82,9 @@
     have hi := congrFun h i
     by_cases hs : s i
     · by_cases ht : t i
-      · simp [cellIndex, hs, ht] at hi
-        exact_mod_cast hi
+      · have hi' : (a i : Int) = b i := by
+          simpa [cellIndex, hs, ht] using hi
+        exact_mod_cast hi'
       · simp [cellIndex, hs, ht] at hi
         have ha : (0 : Int) ≤ (a i : Int) := by omega
         have hb : (0 : Int) ≤ (b i : Int) := by omega
@@ -93,8 +94,9 @@
         have ha : (0 : Int) ≤ (a i : Int) := by omega
         have hb : (0 : Int) ≤ (b i : Int) := by omega
         omega
-      · simp [cellIndex, hs, ht] at hi
-        exact_mod_cast hi
+      · have hi' : (a i : Int) = b i := by
+          simpa [cellIndex, hs, ht] using hi
+        exact_mod_cast hi'
   subst b
   have hst : s = t := by
     funext i
@@ -154,7 +156,10 @@
 
 /-- `K15` is the cardinality of the finite set of valid tuples. -/
 theorem K15_eq_validTuples15_card (X : Nat) : K15 X = (validTuples15 X).card := by
-  simp [K15, validTuples15, valid15]
+  unfold K15 validTuples15
+  apply congrArg Finset.card
+  ext a
+  simp only [Finset.mem_filter, valid15]
 
 /-- Adding all fifteen sign choices multiplies the valid-tuple count by `2 ^ 15`. -/
 theorem card_signedTuples15 (X : Nat) :
@@ -187,11 +192,12 @@
     (ν := cellIndex a s)).mp hx i
   simp only [Nat.cast_one] at hi
   by_cases hs : s i
-  · simp [cellIndex, hs] at hi
+  · simp only [cellIndex, if_pos hs, Int.cast_natCast, div_one] at hi
     have ha : (0 : Real) ≤ a i := by positivity
     rw [abs_of_nonneg (ha.trans hi.1.le)]
     exact hi.2
-  · simp [cellIndex, hs] at hi
+  · simp only [cellIndex, if_neg hs, Int.cast_sub, Int.cast_neg,
+      Int.cast_natCast, Int.cast_one, sub_add_cancel, div_one] at hi
     have ha : (0 : Real) ≤ a i := by positivity
     rw [abs_of_nonpos (by linarith [hi.2])]
     linarith [hi.1]
@@ -205,7 +211,7 @@
       Real.rpow (((a i).val + 1 : Nat) : Real) (5 : Real) := by
     intro i
     have hi := abs_mem_signedCell_le_base hx i
-    simp [finTupleVal] at hi
+    dsimp only [finTupleVal] at hi
     have hi' : |x i| ≤ (((a i).val + 1 : Nat) : Real) := by
       exact_mod_cast hi
     exact Real.rpow_le_rpow (abs_nonneg _) hi' (by positivity)
diff --git a/Warning/Waring/Analytic/ChenTenElevenLocalFactor.lean b/Warning/Waring/Analytic/ChenTenElevenLocalFactor.lean
--- a/Warning/Waring/Analytic/ChenTenElevenLocalFactor.lean
+++ b/Warning/Waring/Analytic/ChenTenElevenLocalFactor.lean
@@ -168,7 +168,7 @@
       ((11 ^ alpha : Nat) : Real) *
         ((((11 ^ (alpha - 1) : Nat) : Real) /
           (11 ^ alpha : Nat)) ^ 15) := by
-  letI : NeZero (11 ^ alpha) := ⟨pow_ne_zero alpha (by norm_num)⟩
+  let : NeZero (11 ^ alpha) := ⟨pow_ne_zero alpha (by norm_num)⟩
   apply norm_chenTenSingularCoefficient_le
   · positivity
   · intro a ha
diff --git a/Warning/Waring/Analytic/ChenTenEulerProduct.lean b/Warning/Waring/Analytic/ChenTenEulerProduct.lean
--- a/Warning/Waring/Analytic/ChenTenEulerProduct.lean
+++ b/Warning/Waring/Analytic/ChenTenEulerProduct.lean
@@ -31,8 +31,8 @@
       have hm_one : m = 1 := m.coprime_zero_right.mp hcoprime
       subst m
       simp
-    · letI : NeZero m := ⟨hm⟩
-      letI : NeZero q := ⟨hq⟩
+    · let : NeZero m := ⟨hm⟩
+      let : NeZero q := ⟨hq⟩
       have hmq : m * q ≠ 0 := Nat.mul_ne_zero hm hq
       simp only [chenTenSingularCoefficientNat, dif_neg hm, dif_neg hq,
         dif_neg hmq]
diff --git a/Warning/Waring/Analytic/ChenTenExceptionalFactors.lean b/Warning/Waring/Analytic/ChenTenExceptionalFactors.lean
--- a/Warning/Waring/Analytic/ChenTenExceptionalFactors.lean
+++ b/Warning/Waring/Analytic/ChenTenExceptionalFactors.lean
@@ -42,7 +42,7 @@
     (hfactor : chenTwoPrimeFactor p <= C) (N : Nat) :
     ‖chenTenPrimeLocalFactor p N - 1‖ <=
       C ^ 15 / ((p : Real) ^ 2 - 1) := by
-  letI : NeZero p := ⟨hp.ne_zero⟩
+  let : NeZero p := ⟨hp.ne_zero⟩
   rw [chenTenPrimeLocalFactor_sub_one_eq_tsum_primePower_succ hp N]
   have hgeom := (hasSum_prime_inv_sq_pow_succ hp).mul_left (C ^ 15)
   have hbound :
diff --git a/Warning/Waring/Analytic/ChenTenFiveCompleteSum.lean b/Warning/Waring/Analytic/ChenTenFiveCompleteSum.lean
--- a/Warning/Waring/Analytic/ChenTenFiveCompleteSum.lean
+++ b/Warning/Waring/Analytic/ChenTenFiveCompleteSum.lean
@@ -16,7 +16,7 @@
 theorem norm_completePowerSum_fifth_twentyFive_le_of_isUnit
     (a : ZMod 25) (ha : IsUnit a) :
     ‖completePowerSum 5 a‖ <= (89 / 5 : Real) := by
-  letI : Fact (1 < 25) := ⟨by norm_num⟩
+  let : Fact (1 < 25) := ⟨by norm_num⟩
   have haZero : a ≠ 0 := ha.ne_zero
   have hPositive : 0 < a.val := ZMod.val_pos.mpr haZero
   have hUpper : a.val < 25 := a.val_lt
@@ -114,7 +114,7 @@
         have hzero := completePowerSum_fifth_five a ha
         rw [show (6 : Nat) = 1 + 5 by norm_num,
           completePowerSum_fifth_pow_add_five 1 a ha, hzero]
-        simp
+        simp only [mul_zero, norm_zero]
         positivity
       · exact norm_completePowerSum_fifth_five_pow_three_to_five_le
           (by omega) (by omega) a ha
diff --git a/Warning/Waring/Analytic/ChenTenFiveLocalFactor.lean b/Warning/Waring/Analytic/ChenTenFiveLocalFactor.lean
--- a/Warning/Waring/Analytic/ChenTenFiveLocalFactor.lean
+++ b/Warning/Waring/Analytic/ChenTenFiveLocalFactor.lean
@@ -31,7 +31,7 @@
     ‖if IsUnit (ZMod.finEquiv 25 a) then
         chenTenSingularTerm 25 N (ZMod.finEquiv 25 a) else 0‖ <=
       fiveTwentyFiveMajorant a := by
-  letI : Fact (1 < 25) := ⟨by norm_num⟩
+  let : Fact (1 < 25) := ⟨by norm_num⟩
   rw [zmod_finEquiv_apply]
   by_cases hExceptional :
       a.val = 3 ∨ a.val = 4 ∨ a.val = 21 ∨ a.val = 22
@@ -86,7 +86,7 @@
 theorem norm_chenTenSingularCoefficientNat_five_sq_le (N : Nat) :
     ‖chenTenSingularCoefficientNat N 25‖ <=
       4 * (89 / 125 : Real) ^ 15 + 16 * (3 / 5 : Real) ^ 15 := by
-  letI : NeZero 25 := ⟨by norm_num⟩
+  let : NeZero 25 := ⟨by norm_num⟩
   have hq : (25 : Nat) ≠ 0 := by norm_num
   simp only [chenTenSingularCoefficientNat, dif_neg hq]
   unfold chenTenSingularCoefficient
@@ -142,7 +142,7 @@
     ‖chenTenSingularCoefficientNat N (5 ^ alpha)‖ <=
       (3 / 2 : Real) ^ 15 *
         (((5 ^ alpha : Nat) : Real) ^ (-2 : Real)) := by
-  letI : NeZero 5 := ⟨by norm_num⟩
+  let : NeZero 5 := ⟨by norm_num⟩
   apply norm_chenTenSingularCoefficientNat_primePow_le_of_completePowerSum
     (3 / 2 : Real) (by norm_num) N
   intro a ha
diff --git a/Warning/Waring/Analytic/ChenTenGenericLocalFactor.lean b/Warning/Waring/Analytic/ChenTenGenericLocalFactor.lean
--- a/Warning/Waring/Analytic/ChenTenGenericLocalFactor.lean
+++ b/Warning/Waring/Analytic/ChenTenGenericLocalFactor.lean
@@ -103,7 +103,7 @@
     (N : Nat) :
     ‖chenTenPrimeLocalFactor p N - 1‖ <=
       1 / ((p : Real) ^ 2 - 1) := by
-  letI : NeZero p := ⟨hp.ne_zero⟩
+  let : NeZero p := ⟨hp.ne_zero⟩
   rw [chenTenPrimeLocalFactor_sub_one_eq_tsum_primePower_succ hp N]
   apply tsum_of_norm_bounded (hasSum_prime_inv_sq_pow_succ hp)
   intro k
diff --git a/Warning/Waring/Analytic/ChenTenMinorPointwise.lean b/Warning/Waring/Analytic/ChenTenMinorPointwise.lean
--- a/Warning/Waring/Analytic/ChenTenMinorPointwise.lean
+++ b/Warning/Waring/Analytic/ChenTenMinorPointwise.lean
@@ -38,7 +38,7 @@
     hqLower, hqUpper, halphaEq, hepsilon⟩ :=
     exists_reducedDirichletApproximation_of_mem_chenTenMinorArcs
       hPpos halpha
-  letI : NeZero q := ⟨Nat.ne_of_gt hqPos⟩
+  let : NeZero q := ⟨Nat.ne_of_gt hqPos⟩
   by_cases hqCut :
       (q : Real) ≤ (P : Real) ^ (26 / 25 : Real)
   · have hsmallRaw :=
diff --git a/Warning/Waring/Analytic/ChenTenSingularCoefficient.lean b/Warning/Waring/Analytic/ChenTenSingularCoefficient.lean
--- a/Warning/Waring/Analytic/ChenTenSingularCoefficient.lean
+++ b/Warning/Waring/Analytic/ChenTenSingularCoefficient.lean
@@ -120,7 +120,7 @@
           norm_div, Complex.norm_natCast, norm_stdAddChar, mul_one]
         exact pow_le_pow_left₀ (by positivity)
           (div_le_div_of_nonneg_right (hB a ha) hq.le) 15
-      · simp [ha]
+      · simp only [if_neg ha, norm_zero]
         positivity
     _ = q * (B / q) ^ 15 := by simp
 
diff --git a/Warning/Waring/Analytic/ChenTenSingularIntegral.lean b/Warning/Waring/Analytic/ChenTenSingularIntegral.lean
--- a/Warning/Waring/Analytic/ChenTenSingularIntegral.lean
+++ b/Warning/Waring/Analytic/ChenTenSingularIntegral.lean
@@ -180,7 +180,7 @@
 private theorem tail_set_eq_outer_intervals {r : Real} :
     {z : Real | r ≤ |z|} = Iic (-r) ∪ Ici r := by
   ext z
-  simp only [mem_setOf_eq, mem_union, mem_Iic, mem_Ici]
+  simp only [mem_ofPred_eq, mem_union, mem_Iic, mem_Ici]
   constructor
   · intro hz
     by_cases hzr : r ≤ z
diff --git a/Warning/Waring/Analytic/ChenTenSingularSeries.lean b/Warning/Waring/Analytic/ChenTenSingularSeries.lean
--- a/Warning/Waring/Analytic/ChenTenSingularSeries.lean
+++ b/Warning/Waring/Analytic/ChenTenSingularSeries.lean
@@ -35,7 +35,6 @@
   by_cases hq : q = 0
   · subst q
     simp [chenTenSingularCoefficientNat]
-    positivity
   · simp only [chenTenSingularCoefficientNat, dif_neg hq]
     exact @norm_chenTenSingularCoefficient_le_chenTwo q ⟨hq⟩ n
 
diff --git a/Warning/Waring/Analytic/ChenTenSingularSeriesReindex.lean b/Warning/Waring/Analytic/ChenTenSingularSeriesReindex.lean
--- a/Warning/Waring/Analytic/ChenTenSingularSeriesReindex.lean
+++ b/Warning/Waring/Analytic/ChenTenSingularSeriesReindex.lean
@@ -123,7 +123,7 @@
       have hmul : q * q ≤ P := by
         simpa [Q] using (Nat.le_sqrt.mp (by simpa [Q] using hqQ))
       simpa [pow_two] using hmul
-    letI : NeZero q := ⟨Nat.ne_of_gt hqpos⟩
+    let : NeZero q := ⟨Nat.ne_of_gt hqpos⟩
     rw [sum_chenTenIndexedSingularTerm_fiber P N q hqpos hqcut]
     simp [chenTenSingularCoefficientNat, hq0]
 
diff --git a/Warning/Waring/Analytic/ChenTenSingularSeriesTail.lean b/Warning/Waring/Analytic/ChenTenSingularSeriesTail.lean
--- a/Warning/Waring/Analytic/ChenTenSingularSeriesTail.lean
+++ b/Warning/Waring/Analytic/ChenTenSingularSeriesTail.lean
@@ -85,7 +85,6 @@
           rw [show (-2 : Real) = -(2 : Nat) by norm_num,
             Real.rpow_neg_natCast]
           norm_num
-          rfl
         calc
           ‖f (k + Q + 1)‖ <=
               (40 : Real) ^ 15 *
diff --git a/Warning/Waring/Analytic/ChenThree.lean b/Warning/Waring/Analytic/ChenThree.lean
--- a/Warning/Waring/Analytic/ChenThree.lean
+++ b/Warning/Waring/Analytic/ChenThree.lean
@@ -308,7 +308,7 @@
 coefficient, as stated separately in Chen's Lemma 3. -/
 theorem completePowerSum_fifth_five (a : Nat) (ha : a.Coprime 5) :
     completePowerSum 5 ((a : Nat) : ZMod 5) = 0 := by
-  letI : Fact (Nat.Prime 5) := ⟨Nat.prime_five⟩
+  let : Fact (Nat.Prime 5) := ⟨Nat.prime_five⟩
   have hpow (x : ZMod 5) : x ^ 5 = x := ZMod.pow_card x
   have haNonzero : ((a : Nat) : ZMod 5) ≠ 0 := by
     rw [ne_eq, ZMod.natCast_eq_zero_iff]
diff --git a/Warning/Waring/Analytic/ChenTwo.lean b/Warning/Waring/Analytic/ChenTwo.lean
--- a/Warning/Waring/Analytic/ChenTwo.lean
+++ b/Warning/Waring/Analytic/ChenTwo.lean
@@ -99,7 +99,7 @@
   have hAll : ∀ n, motive n := by
     apply Nat.recOnPosPrimePosCoprime
     · intro p alpha hp hAlpha hn b hb
-      letI : NeZero p := ⟨hp.ne_zero⟩
+      let : NeZero p := ⟨hp.ne_zero⟩
       simpa [chenTwoFactor, primeFactors_primePow hp hAlpha] using
         chen_two_primePower hp hAlpha b hb
     · intro hn
@@ -110,8 +110,8 @@
     · intro m n hmOne hnOne hcoprime hmInd hnInd hmn b hb
       have hm : m ≠ 0 := by omega
       have hn : n ≠ 0 := by omega
-      letI : NeZero m := ⟨hm⟩
-      letI : NeZero n := ⟨hn⟩
+      let : NeZero m := ⟨hm⟩
+      let : NeZero n := ⟨hn⟩
       let leftCoefficient : ZMod m :=
         (n : ZMod m)⁻¹ * (ZMod.chineseRemainder hcoprime b).1
       let rightCoefficient : ZMod n :=
diff --git a/Warning/Waring/Analytic/DiophantineMinimumSum.lean b/Warning/Waring/Analytic/DiophantineMinimumSum.lean
--- a/Warning/Waring/Analytic/DiophantineMinimumSum.lean
+++ b/Warning/Waring/Analytic/DiophantineMinimumSum.lean
@@ -23,7 +23,7 @@
   by_cases hh : h = 0
   · simp [diophantineMinWeight, hh]
   · simp only [diophantineMinWeight, hh, ↓reduceIte]
-    exact le_min (by positivity) (by simp [leastResidueWeight]; positivity)
+    exact le_min (by positivity) (by dsimp only [leastResidueWeight]; positivity)
 
 /-- One complete residue system costs at most its zero term plus the paired
 harmonic least-residue sum. -/
diff --git a/Warning/Waring/Analytic/FiniteFieldTraceInstances.lean b/Warning/Waring/Analytic/FiniteFieldTraceInstances.lean
--- a/Warning/Waring/Analytic/FiniteFieldTraceInstances.lean
+++ b/Warning/Waring/Analytic/FiniteFieldTraceInstances.lean
@@ -55,7 +55,7 @@
   have hbase :
       baseAlgebraMap (p := p) (n := n) (baseTrace (p := p) (n := n) x) =
         ∑ i ∈ Finset.range n, x ^ p ^ i := by
-    letI : Algebra (ZMod p) (FiniteField.Extension (ZMod p) p n) :=
+    let : Algebra (ZMod p) (FiniteField.Extension (ZMod p) p n) :=
       FiniteField.instAlgebraExtension (ZMod p) p n
     dsimp [baseAlgebraMap, baseTrace]
     rw [FiniteField.algebraMap_trace_eq_sum_pow,
@@ -63,7 +63,7 @@
   have hprime :
       primeAlgebraMap (p := p) (n := n) (primeTrace (p := p) (n := n) x) =
         ∑ i ∈ Finset.range n, x ^ p ^ i := by
-    letI : Algebra (ZMod p) (FiniteField.Extension (ZMod p) p n) :=
+    let : Algebra (ZMod p) (FiniteField.Extension (ZMod p) p n) :=
       FiniteField.instAlgebraZModExtension (ZMod p) p n
     dsimp [primeAlgebraMap, primeTrace]
     rw [FiniteField.algebraMap_trace_eq_sum_pow,
diff --git a/Warning/Waring/Analytic/FivePrimePower.lean b/Warning/Waring/Analytic/FivePrimePower.lean
--- a/Warning/Waring/Analytic/FivePrimePower.lean
+++ b/Warning/Waring/Analytic/FivePrimePower.lean
@@ -164,7 +164,8 @@
     _ = 625 * ∑ y : Fin u,
         ZMod.stdAddChar ((a * y.val ^ 5 : Nat) : ZMod u) := by
       simp only [Finset.mul_sum]
-      simp
+      simp only [Nat.cast_mul, Nat.cast_pow, Finset.sum_const, Finset.card_univ,
+        Fintype.card_fin, nsmul_eq_mul, Nat.cast_ofNat]
       rw [← Finset.mul_sum]
       rw [← Finset.mul_sum]
       ring
diff --git a/Warning/Waring/Analytic/FourierCoefficientSum.lean b/Warning/Waring/Analytic/FourierCoefficientSum.lean
--- a/Warning/Waring/Analytic/FourierCoefficientSum.lean
+++ b/Warning/Waring/Analytic/FourierCoefficientSum.lean
@@ -76,7 +76,7 @@
     (∑ h : ZMod (2 * n + 1), leastResidueWeight (2 * n + 1) h) ≤
       (2 * n + 1 : Nat) * (harmonic n : Real) := by
   let q : Nat := 2 * n + 1
-  haveI : NeZero q := ⟨by dsimp [q]; omega⟩
+  have : NeZero q := ⟨by dsimp [q]; omega⟩
   let distance : ZMod q → Nat := fun h ↦ h.valMinAbs.natAbs
   have hqHalf : q / 2 = n := by dsimp [q]; omega
   have hmap : ∀ h ∈ (Finset.univ : Finset (ZMod q)),
@@ -131,7 +131,7 @@
     (∑ h : ZMod (2 * (n + 1)), leastResidueWeight (2 * (n + 1)) h) ≤
       (2 * (n + 1) : Nat) * (harmonic n : Real) + 1 := by
   let q : Nat := 2 * (n + 1)
-  haveI : NeZero q := ⟨by dsimp [q]; omega⟩
+  have : NeZero q := ⟨by dsimp [q]; omega⟩
   let distance : ZMod q → Nat := fun h ↦ h.valMinAbs.natAbs
   have hqHalf : q / 2 = n + 1 := by dsimp [q]; omega
   have hmap : ∀ h ∈ (Finset.univ : Finset (ZMod q)),
diff --git a/Warning/Waring/Analytic/HuaSmallPrimePower.lean b/Warning/Waring/Analytic/HuaSmallPrimePower.lean
--- a/Warning/Waring/Analytic/HuaSmallPrimePower.lean
+++ b/Warning/Waring/Analytic/HuaSmallPrimePower.lean
@@ -53,9 +53,9 @@
     ‖@fivePolynomialCompleteSum (p ^ alpha)
         ⟨pow_ne_zero alpha hp.ne_zero⟩ a‖ ≤
       125 * (((p ^ alpha : Nat) : Real) ^ (4 / 5 : Real)) := by
-  letI : Fact p.Prime := ⟨hp⟩
-  letI : NeZero p := ⟨hp.ne_zero⟩
-  letI : NeZero (p ^ alpha) := ⟨pow_ne_zero alpha hp.ne_zero⟩
+  let : Fact p.Prime := ⟨hp⟩
+  let : NeZero p := ⟨hp.ne_zero⟩
+  let : NeZero (p ^ alpha) := ⟨pow_ne_zero alpha hp.ne_zero⟩
   rw [fivePolynomialCompleteSum_eq_integerRepresentatives]
   rw [← integerFormalPolynomialCompleteSum_fifthPolynomialFormal]
   apply hua_fifthPolynomialFormal_primePower_bound p alpha hpSmall hAlpha
diff --git a/Warning/Waring/Analytic/NonfivePrimePower.lean b/Warning/Waring/Analytic/NonfivePrimePower.lean
--- a/Warning/Waring/Analytic/NonfivePrimePower.lean
+++ b/Warning/Waring/Analytic/NonfivePrimePower.lean
@@ -191,7 +191,7 @@
 theorem sum_fin_prime_dvd (p s : Nat) (hp : p.Prime) :
     ∑ eta : Fin (p * s),
       (if p ∣ eta.val then (p : Complex) else 0) = p * s := by
-  letI : NeZero p := ⟨hp.ne_zero⟩
+  let : NeZero p := ⟨hp.ne_zero⟩
   rw [← (finPrimeBlocks p s).sum_comp]
   rw [Fintype.sum_prod_type]
   calc
@@ -372,7 +372,8 @@
     _ = (p ^ 4 : Nat) * ∑ y : Fin u,
         ZMod.stdAddChar ((a * y.val ^ 5 : Nat) : ZMod u) := by
       simp only [Finset.mul_sum]
-      simp
+      simp only [Nat.cast_mul, Nat.cast_pow, Finset.sum_const, Finset.card_univ,
+        Fintype.card_fin, nsmul_eq_mul]
       rw [← Finset.mul_sum]
       rw [← Finset.mul_sum]
       ring
diff --git a/Warning/Waring/Analytic/PowerSeriesRecurrence.lean b/Warning/Waring/Analytic/PowerSeriesRecurrence.lean
--- a/Warning/Waring/Analytic/PowerSeriesRecurrence.lean
+++ b/Warning/Waring/Analytic/PowerSeriesRecurrence.lean
@@ -18,7 +18,7 @@
 /-- Taking the coefficient of degree `n` in `X * L' = L * B` gives the
 Cauchy-product recurrence, written over the natural-number antidiagonal. -/
 theorem coeff_mul_nat_eq_sum_antidiagonal_of_X_mul_derivativeFun_eq_mul
-    {L B : R⟦X⟧} (h : X * L.derivativeFun = L * B) (n : Nat) :
+    {L B : R⟦X⟧} (h : X * (derivative R L) = L * B) (n : Nat) :
     coeff n L * (n : R) =
       ∑ p ∈ Finset.antidiagonal n, coeff p.1 L * coeff p.2 B := by
   cases n with
@@ -28,7 +28,7 @@
       simpa only [Nat.cast_zero, mul_zero] using hn
   | succ n =>
       have hn := congrArg (coeff (n + 1)) h
-      rw [coeff_succ_X_mul, coeff_derivativeFun, coeff_mul] at hn
+      rw [coeff_succ_X_mul, coeff_derivative, coeff_mul] at hn
       simpa only [Nat.cast_add, Nat.cast_one] using hn
 
 /-- Range-indexed coefficient recurrence obtained from `X * L' = L * B`.
@@ -36,7 +36,7 @@
 usual Euler-product recurrence
 `n L_n = \sum_{i=0}^n L_{n-i} B_i`. -/
 theorem nat_mul_coeff_eq_sum_range_of_X_mul_derivativeFun_eq_mul
-    {L B : R⟦X⟧} (h : X * L.derivativeFun = L * B) (n : Nat) :
+    {L B : R⟦X⟧} (h : X * (derivative R L) = L * B) (n : Nat) :
     (n : R) * coeff n L =
       ∑ i ∈ Finset.range (n + 1), coeff (n - i) L * coeff i B := by
   rw [mul_comm]
@@ -49,7 +49,7 @@
 `X * L' = L * B` only involves the positive-degree coefficients of `B`:
 `n L_n = \sum_{i=1}^n L_{n-i} B_i`. -/
 theorem nat_mul_coeff_eq_sum_positive_of_X_mul_derivativeFun_eq_mul
-    {L B : R⟦X⟧} (h : X * L.derivativeFun = L * B)
+    {L B : R⟦X⟧} (h : X * (derivative R L) = L * B)
     (hB : constantCoeff B = 0) (n : Nat) :
     (n : R) * coeff n L =
       ∑ i ∈ Finset.range n, coeff (n - (i + 1)) L * coeff (i + 1) B := by
@@ -62,8 +62,8 @@
 one.  This comparison avoids division by coefficient indices. -/
 theorem coeff_logDerivative_eq_of_coeff_eq_up_to
     [IsLeftCancelAdd R] {L A B C : R⟦X⟧} {N : Nat}
-    (hLB : X * L.derivativeFun = L * B)
-    (hAC : X * A.derivativeFun = A * C)
+    (hLB : X * (derivative R L) = L * B)
+    (hAC : X * (derivative R A) = A * C)
     (hB0 : constantCoeff B = 0) (hC0 : constantCoeff C = 0)
     (hL0 : coeff 0 L = 1)
     (hcoeff : ∀ j, j ≤ N → coeff j L = coeff j A)
diff --git a/Warning/Waring/Analytic/PrimeGauss.lean b/Warning/Waring/Analytic/PrimeGauss.lean
--- a/Warning/Waring/Analytic/PrimeGauss.lean
+++ b/Warning/Waring/Analytic/PrimeGauss.lean
@@ -12,7 +12,7 @@
 
 namespace Waring.Analytic
 
-open AddChar MulChar
+open _root_.Waring.Analytic.AddChar MulChar
 open scoped BigOperators
 
 /-- If five is coprime to the order of the unit group, fifth powering
@@ -20,7 +20,7 @@
 theorem fifthPower_bijective_zmod {p : Nat} (hp : p.Prime)
     (hFifth : (p - 1).Coprime 5) :
     Function.Bijective (fun x : ZMod p => x ^ 5) := by
-  letI : Fact p.Prime := ⟨hp⟩
+  let : Fact p.Prime := ⟨hp⟩
   have hcard : Nat.card (ZMod p)ˣ = p - 1 := by
     rw [Nat.card_eq_fintype_card, ZMod.card_units_eq_totient,
       Nat.totient_prime hp]
@@ -59,7 +59,7 @@
     (hp : p.Prime) (hFifth : (p - 1).Coprime 5)
     (ha : a.Coprime p) :
     completePowerSum 5 ((a : Nat) : ZMod p) = 0 := by
-  letI : Fact p.Prime := ⟨hp⟩
+  let : Fact p.Prime := ⟨hp⟩
   have hbij := fifthPower_bijective_zmod hp hFifth
   rw [completePowerSum, powerSum]
   calc
@@ -92,18 +92,19 @@
 
 /-- A fifth-power additive-character sum over a finite field is a sum of four
 nontrivial Gauss sums when five divides the unit-group order. -/
-theorem fifthPower_sum_bound {F : Type*} [Field F] [Fintype F] [DecidableEq F]
+theorem fifthPower_sum_bound {F : Type*} [Field F] [Fintype F]
     (hFive : 5 ∣ Fintype.card F - 1)
     {psi : AddChar F Complex} (hpsi : psi.IsPrimitive) :
     ‖∑ x : F, psi (x ^ 5)‖ ≤
       4 * Real.sqrt (Fintype.card F) := by
+  classical
   let fifthHom : Fˣ →* Fˣ := powMonoidHom 5
   let H : Subgroup Fˣ := fifthHom.range
   let X : Subgroup (MulChar F Complex) :=
     (MulChar.subgroupOrderIsoSubgroupMulChar F Complex H).ofDual
-  letI : Fintype H := Fintype.ofFinite H
-  letI : Fintype X := Fintype.ofFinite X
-  letI : DecidableEq X := Classical.decEq X
+  let : Fintype H := Fintype.ofFinite H
+  let : Fintype X := Fintype.ofFinite X
+  let : DecidableEq X := Classical.decEq X
   have hcardUnits : Nat.card Fˣ = Fintype.card F - 1 := by
     rw [Nat.card_eq_fintype_card, Fintype.card_units]
   have hcardX : Fintype.card X = 5 := by
@@ -303,7 +304,7 @@
     (hp : p.Prime) (hFive : 5 ∣ p - 1) (ha : a.Coprime p) :
     ‖completePowerSum 5 ((a : Nat) : ZMod p)‖ ≤
       4 * Real.sqrt p := by
-  letI : Fact p.Prime := ⟨hp⟩
+  let : Fact p.Prime := ⟨hp⟩
   let psi : AddChar (ZMod p) Complex :=
     ZMod.stdAddChar.mulShift ((a : Nat) : ZMod p)
   have haZero : ((a : Nat) : ZMod p) ≠ 0 := by
diff --git a/Warning/Waring/Analytic/ReducedRationalGrid.lean b/Warning/Waring/Analytic/ReducedRationalGrid.lean
--- a/Warning/Waring/Analytic/ReducedRationalGrid.lean
+++ b/Warning/Waring/Analytic/ReducedRationalGrid.lean
@@ -171,7 +171,7 @@
   let Q := reducedGridDenominator c q
   let C := reducedGridCoefficient c q
   have hd : 0 < d := rationalGridGcd_pos c q
-  letI : NeZero d := ⟨hd.ne'⟩
+  let : NeZero d := ⟨hd.ne'⟩
   have hq : d * Q = q := rationalGridGcd_mul_reducedGridDenominator c q
   have hc : d * C = c := rationalGridGcd_mul_reducedGridCoefficient c q
   have hscale := diophantineMinWeight_scaled_modulus d Q P ((C : Int) * m)
diff --git a/Warning/Waring/Analytic/StepanovExtensionSumBound.lean b/Warning/Waring/Analytic/StepanovExtensionSumBound.lean
--- a/Warning/Waring/Analytic/StepanovExtensionSumBound.lean
+++ b/Warning/Waring/Analytic/StepanovExtensionSumBound.lean
@@ -33,12 +33,12 @@
         (Weil.pointPhase ((algebraMap (ZMod p) E) ∘ b) x))‖ ≤
       (p * (d * (p - 1) * p + 1) * p ^ (h + 3) : Nat) := by
   classical
-  letI : NeZero p := ⟨by omega⟩
-  letI : NeZero (2 * (h + 3)) := ⟨by omega⟩
+  let : NeZero p := ⟨by omega⟩
+  let : NeZero (2 * (h + 3)) := ⟨by omega⟩
   let E := FiniteField.Extension (ZMod p) p (2 * (h + 3))
-  letI : CharP E p :=
+  let : CharP E p :=
     (Algebra.charP_iff (ZMod p) E p).mp (ZMod.charP p)
-  letI : Fintype E := Fintype.ofFinite E
+  let : Fintype E := Fintype.ofFinite E
   let phaseTrace : E → ZMod p := fun x ↦
     Algebra.trace (ZMod p) E
       (Weil.pointPhase ((algebraMap (ZMod p) E) ∘ b) x)
diff --git a/Warning/Waring/Analytic/StepanovTraceFiberBound.lean b/Warning/Waring/Analytic/StepanovTraceFiberBound.lean
--- a/Warning/Waring/Analytic/StepanovTraceFiberBound.lean
+++ b/Warning/Waring/Analytic/StepanovTraceFiberBound.lean
@@ -32,9 +32,9 @@
       traceFiberBound p h d := by
   classical
   let E := FiniteField.Extension (ZMod p) p (2 * (h + 3))
-  letI : CharP E p :=
+  let : CharP E p :=
     (Algebra.charP_iff (ZMod p) E p).mp (ZMod.charP p)
-  letI : Fintype E := Fintype.ofFinite E
+  let : Fintype E := Fintype.ofFinite E
   let f : E[X] := mappedPointPhasePolynomial (E := E) b
   let points : Finset E := Finset.univ.filter fun x : E ↦
     Algebra.trace (ZMod p) E
diff --git a/Warning/Waring/Analytic/StepanovTracePointPhase.lean b/Warning/Waring/Analytic/StepanovTracePointPhase.lean
--- a/Warning/Waring/Analytic/StepanovTracePointPhase.lean
+++ b/Warning/Waring/Analytic/StepanovTracePointPhase.lean
@@ -153,7 +153,7 @@
             ((algebraMap (ZMod p)
               (FiniteField.Extension (ZMod p) p (2 * (h + 3)))) ∘ b) x)) := by
   let E := FiniteField.Extension (ZMod p) p (2 * (h + 3))
-  letI : CharP E p :=
+  let : CharP E p :=
     (Algebra.charP_iff (ZMod p) E p).mp (ZMod.charP p)
   apply eval_fullTracePolynomial_pointPhase_eq_algebraMap_trace
   simpa using
diff --git a/Warning/Waring/Analytic/WeilArtinPolynomial.lean b/Warning/Waring/Analytic/WeilArtinPolynomial.lean
--- a/Warning/Waring/Analytic/WeilArtinPolynomial.lean
+++ b/Warning/Waring/Analytic/WeilArtinPolynomial.lean
@@ -39,7 +39,7 @@
     monicFormalRootWeightSum character b 0 = 1 := by
   let oneMonic : {F : K[X] // F.Monic ∧ F.natDegree = 0} :=
     ⟨1, monic_one, natDegree_one⟩
-  letI : Unique {F : K[X] // F.Monic ∧ F.natDegree = 0} :=
+  let : Unique {F : K[X] // F.Monic ∧ F.natDegree = 0} :=
     { default := oneMonic
       uniq := by
         intro F
diff --git a/Warning/Waring/Analytic/WeilChenAdapter.lean b/Warning/Waring/Analytic/WeilChenAdapter.lean
--- a/Warning/Waring/Analytic/WeilChenAdapter.lean
+++ b/Warning/Waring/Analytic/WeilChenAdapter.lean
@@ -123,8 +123,8 @@
 /-- The large-prime hypothesis consumed by Chen's Lemma 5 is unconditional. -/
 theorem chenFiveLargePrimeFieldBound : ChenFiveLargePrimeFieldBound := by
   intro p hp hpLarge
-  letI : Fact p.Prime := ⟨hp⟩
-  letI : NeZero p := ⟨hp.ne_zero⟩
+  let : Fact p.Prime := ⟨hp⟩
+  let : NeZero p := ⟨hp.ne_zero⟩
   exact chenFourPrimeFieldBound_of_squareRootBound p
     (chenFourPrimeFieldSquareRootBound_of_five_lt p (by omega))
 
diff --git a/Warning/Waring/Analytic/WeilClosedPoints.lean b/Warning/Waring/Analytic/WeilClosedPoints.lean
--- a/Warning/Waring/Analytic/WeilClosedPoints.lean
+++ b/Warning/Waring/Analytic/WeilClosedPoints.lean
@@ -71,10 +71,11 @@
   apply ConjRootClass.minpoly_injective
   exact congrArg (fun P : ExtensionClosedPoint K n ↦ P.poly) h
 
-private theorem exists_minpoly_eq_closedPoint
+omit [Fintype K] in
+private theorem exists_minpoly_eq_closedPoint [Finite K]
     (P : ExtensionClosedPoint K n) :
     ∃ x : FiniteField.Extension K p n, minpoly K x = P.poly := by
-  letI : Fact (Irreducible P.poly) := ⟨P.irreducible⟩
+  let : Fact (Irreducible P.poly) := ⟨P.irreducible⟩
   have hfinrank : Module.finrank K (AdjoinRoot P.poly) = P.poly.natDegree := by
     rw [(AdjoinRoot.powerBasis P.irreducible.ne_zero).finrank,
       AdjoinRoot.powerBasis_dim]
diff --git a/Warning/Waring/Analytic/WeilEulerCoefficients.lean b/Warning/Waring/Analytic/WeilEulerCoefficients.lean
--- a/Warning/Waring/Analytic/WeilEulerCoefficients.lean
+++ b/Warning/Waring/Analytic/WeilEulerCoefficients.lean
@@ -267,6 +267,7 @@
   rw [monicFactorProduct_factorization]
   rfl
 
+omit [DecidableEq K] in
 /-- For the formal-root local weights, the coefficient expansion is the sum
 of formal-root weights over monic polynomials of the requested degree. -/
 theorem coeff_finiteEulerProduct_formalRootWeight_eq_sum_monic
@@ -277,12 +278,14 @@
             (formalRootWeight character b P.poly)) =
       ∑ F : {F : K[X] // F.Monic ∧ F.natDegree = n},
         formalRootWeight character b F.1 := by
+  classical
   rw [coeff_finiteEulerProduct_eq_sum_monic hnN]
   apply Finset.sum_congr rfl
   intro F hF
   rw [multiplicativeFactorWeight_formalRootWeight,
     monicFactorProduct_factorization]
 
+omit [DecidableEq K] in
 /-- All coefficients from the highest phase degree through the Euler cutoff
 vanish.  This is the finite, division-free polynomiality statement used in
 the Artin `L`-function argument. -/
diff --git a/Warning/Waring/Analytic/WeilEulerRootIdentity.lean b/Warning/Waring/Analytic/WeilEulerRootIdentity.lean
--- a/Warning/Waring/Analytic/WeilEulerRootIdentity.lean
+++ b/Warning/Waring/Analytic/WeilEulerRootIdentity.lean
@@ -17,7 +17,7 @@
 
 namespace Weil
 
-variable {K : Type*} [Field K] [Fintype K] [DecidableEq K]
+variable {K : Type*} [Field K] [Fintype K]
 
 /-- Through the cutoff `N`, the bounded Euler product has exactly the
 coefficients of the fixed Artin coefficient polynomial. -/
diff --git a/Warning/Waring/Analytic/WeilFiniteEuler.lean b/Warning/Waring/Analytic/WeilFiniteEuler.lean
--- a/Warning/Waring/Analytic/WeilFiniteEuler.lean
+++ b/Warning/Waring/Analytic/WeilFiniteEuler.lean
@@ -35,23 +35,23 @@
 private theorem X_mul_derivativeFun_prod_localEuler
     {I : Type*} [Fintype I] (e : I → Nat) (he : ∀ i, e i ≠ 0)
     (z : I → R) :
-    PowerSeries.X * PowerSeries.derivativeFun
+    PowerSeries.X * (PowerSeries.derivative R)
         (∏ i : I, localEuler (e i) (z i)) =
       (∏ i : I, localEuler (e i) (z i)) *
         (∑ i : I, PowerSeries.C (e i : R) *
           (localEuler (e i) (z i) - 1)) := by
   classical
   let s : Finset I := Finset.univ
-  change PowerSeries.X * PowerSeries.derivativeFun
+  change PowerSeries.X * (PowerSeries.derivative R)
       (∏ i ∈ s, localEuler (e i) (z i)) =
     (∏ i ∈ s, localEuler (e i) (z i)) *
       (∑ i ∈ s, PowerSeries.C (e i : R) *
         (localEuler (e i) (z i) - 1))
   induction s using Finset.induction_on with
-  | empty => simp [PowerSeries.derivativeFun_one]
+  | empty => simp
   | @insert a s ha ih =>
       rw [Finset.prod_insert ha, Finset.sum_insert ha,
-        PowerSeries.derivativeFun_mul]
+        Derivation.leibniz]
       simp only [smul_eq_mul]
       have hlocal := X_mul_derivativeFun_localEuler (z a) (he a)
       linear_combination
@@ -63,7 +63,7 @@
 theorem X_mul_derivativeFun_finiteEulerProduct
     (w : MonicIrreducibleLE K N → R) :
     PowerSeries.X *
-        PowerSeries.derivativeFun (finiteEulerProduct w) =
+        (PowerSeries.derivative R) (finiteEulerProduct w) =
       finiteEulerProduct w * finiteEulerLogDerivative w := by
   rw [finiteEulerProduct, finiteEulerLogDerivative]
   exact X_mul_derivativeFun_prod_localEuler
diff --git a/Warning/Waring/Analytic/WeilLocalEuler.lean b/Warning/Waring/Analytic/WeilLocalEuler.lean
--- a/Warning/Waring/Analytic/WeilLocalEuler.lean
+++ b/Warning/Waring/Analytic/WeilLocalEuler.lean
@@ -50,11 +50,9 @@
   exact hsubst
 
 private theorem derivative_one_sub_C_mul_X_pow (z : R) (e : Nat) :
-    PowerSeries.derivativeFun
+    (PowerSeries.derivative R)
         (1 - PowerSeries.C z * PowerSeries.X ^ e) =
       -PowerSeries.C ((e : R) * z) * PowerSeries.X ^ (e - 1) := by
-  change PowerSeries.derivative R
-      (1 - PowerSeries.C z * PowerSeries.X ^ e) = _
   rw [map_sub, ← map_one (PowerSeries.C (R := R)),
     PowerSeries.derivative_C, Derivation.leibniz,
     PowerSeries.derivative_C, PowerSeries.derivative_pow,
@@ -67,21 +65,21 @@
 /-- The division-free logarithmic derivative identity for a local Euler
 factor: `X L' = e L (L - 1)`. -/
 theorem X_mul_derivativeFun_localEuler (z : R) {e : Nat} (he : e ≠ 0) :
-    PowerSeries.X * PowerSeries.derivativeFun (localEuler e z) =
+    PowerSeries.X * (PowerSeries.derivative R) (localEuler e z) =
       PowerSeries.C (e : R) * localEuler e z * (localEuler e z - 1) := by
   let L : PowerSeries R := localEuler e z
   let Q : PowerSeries R :=
     1 - PowerSeries.C z * PowerSeries.X ^ e
   have hInv : L * Q = 1 := localEuler_mul_one_sub z he
   have hDer :
-      L * PowerSeries.derivativeFun Q +
-          Q * PowerSeries.derivativeFun L = 0 := by
-    have h := congrArg PowerSeries.derivativeFun hInv
-    rw [PowerSeries.derivativeFun_mul,
-      PowerSeries.derivativeFun_one] at h
+      L * (PowerSeries.derivative R) Q +
+          Q * (PowerSeries.derivative R) L = 0 := by
+    have h := congrArg (PowerSeries.derivative R) hInv
+    rw [Derivation.leibniz,
+      PowerSeries.derivative_one] at h
     simpa only [smul_eq_mul, add_comm] using h
   have hQDer :
-      PowerSeries.derivativeFun Q =
+      (PowerSeries.derivative R) Q =
         -PowerSeries.C ((e : R) * z) *
           PowerSeries.X ^ (e - 1) :=
     derivative_one_sub_C_mul_X_pow z e
@@ -99,7 +97,7 @@
   rw [map_natCast]
   linear_combination
     (PowerSeries.X * L) * hDer -
-      (PowerSeries.X * PowerSeries.derivativeFun L +
+      (PowerSeries.X * (PowerSeries.derivative R) L +
         (e : PowerSeries R) * L) * hInv +
       ((e : PowerSeries R) * PowerSeries.C z * L ^ 2) * hPow
 
diff --git a/Warning/Waring/Analytic/WeilMonicFactors.lean b/Warning/Waring/Analytic/WeilMonicFactors.lean
--- a/Warning/Waring/Analytic/WeilMonicFactors.lean
+++ b/Warning/Waring/Analytic/WeilMonicFactors.lean
@@ -139,6 +139,7 @@
       exact P.monic.normalize_eq_self
     _ = s := Multiset.map_id s
 
+omit [DecidableEq K] in
 /-- The represented product map is injective. -/
 theorem monicFactorProduct_injective :
     Function.Injective
diff --git a/Warning/Waring/Analytic/WeilPrimeFieldBound.lean b/Warning/Waring/Analytic/WeilPrimeFieldBound.lean
--- a/Warning/Waring/Analytic/WeilPrimeFieldBound.lean
+++ b/Warning/Waring/Analytic/WeilPrimeFieldBound.lean
@@ -26,7 +26,7 @@
     (hb : ∀ i : Fin 5, d < i.val + 1 → b i = 0) :
     ‖∑ x : ZMod p, ZMod.stdAddChar (pointPhase b x)‖ ≤
       ((d - 1 : Nat) : Real) * Real.sqrt (p : Real) := by
-  letI : NeZero p := ⟨by omega⟩
+  let : NeZero p := ⟨by omega⟩
   have hcharacter :
       (ZMod.stdAddChar : AddChar (ZMod p) Complex) ≠ 1 := by
     intro htrivial
diff --git a/Warning/Waring/Analytic/WeilPrimeFieldIdentity.lean b/Warning/Waring/Analytic/WeilPrimeFieldIdentity.lean
--- a/Warning/Waring/Analytic/WeilPrimeFieldIdentity.lean
+++ b/Warning/Waring/Analytic/WeilPrimeFieldIdentity.lean
@@ -28,7 +28,7 @@
       -((artinLPolynomial character b d).reverse.roots.map
         fun z ↦ z ^ 1).sum := by
   let E := FiniteField.Extension K p 1
-  letI : Fintype E := Fintype.ofFinite E
+  let : Fintype E := Fintype.ofFinite E
   let e : K ≃ₐ[K] E :=
     FiniteField.algEquivExtension K p 1 K (by simp)
   have hphase (x : K) :
diff --git a/Warning/Waring/Analytic/WeilReciprocalRoots.lean b/Warning/Waring/Analytic/WeilReciprocalRoots.lean
--- a/Warning/Waring/Analytic/WeilReciprocalRoots.lean
+++ b/Warning/Waring/Analytic/WeilReciprocalRoots.lean
@@ -55,13 +55,13 @@
 factor. -/
 theorem X_mul_derivativeFun_reciprocalRootFactor (a : Complex) :
     PowerSeries.X *
-        PowerSeries.derivativeFun (reciprocalRootFactor a) =
+        (PowerSeries.derivative Complex) (reciprocalRootFactor a) =
       reciprocalRootFactor a * reciprocalRootLogTerm a := by
   have hinverse : localEuler 1 a * reciprocalRootFactor a = 1 := by
     simpa only [reciprocalRootFactor, pow_one] using
       (localEuler_mul_one_sub (R := Complex) a (e := 1) one_ne_zero)
   have hderivative :
-      PowerSeries.derivativeFun (reciprocalRootFactor a) =
+      (PowerSeries.derivative Complex) (reciprocalRootFactor a) =
         PowerSeries.C (-a) := by
     change PowerSeries.derivative Complex
       (1 - PowerSeries.C a * PowerSeries.X) = _
@@ -98,29 +98,28 @@
 /-- The reciprocal-root product satisfies `X A' = A C`. -/
 theorem X_mul_derivativeFun_reciprocalRootProduct
     (s : Multiset Complex) :
-    PowerSeries.X * PowerSeries.derivativeFun (reciprocalRootProduct s) =
+    PowerSeries.X * (PowerSeries.derivative Complex) (reciprocalRootProduct s) =
       reciprocalRootProduct s * reciprocalRootLogDerivative s := by
   induction s using Multiset.induction_on with
   | empty =>
-      simp [reciprocalRootProduct, reciprocalRootLogDerivative,
-        PowerSeries.derivativeFun_one]
+      simp [reciprocalRootProduct, reciprocalRootLogDerivative]
   | cons a s ih =>
       rw [reciprocalRootProduct_cons,
         reciprocalRootLogDerivative_cons]
-      rw [PowerSeries.derivativeFun_mul]
+      rw [Derivation.leibniz]
       simp only [smul_eq_mul]
       calc
         PowerSeries.X *
             (reciprocalRootFactor a *
-                PowerSeries.derivativeFun (reciprocalRootProduct s) +
+                (PowerSeries.derivative Complex) (reciprocalRootProduct s) +
               reciprocalRootProduct s *
-                PowerSeries.derivativeFun (reciprocalRootFactor a)) =
+                (PowerSeries.derivative Complex) (reciprocalRootFactor a)) =
             reciprocalRootFactor a *
                 (PowerSeries.X *
-                  PowerSeries.derivativeFun (reciprocalRootProduct s)) +
+                  (PowerSeries.derivative Complex) (reciprocalRootProduct s)) +
               reciprocalRootProduct s *
                 (PowerSeries.X *
-                  PowerSeries.derivativeFun (reciprocalRootFactor a)) := by
+                  (PowerSeries.derivative Complex) (reciprocalRootFactor a)) := by
           rw [mul_add]
           congr 1 <;> ac_rfl
         _ = reciprocalRootFactor a *
@@ -187,11 +186,15 @@
           (fun a ↦ ((1 - Polynomial.C a * Polynomial.X :
             Polynomial Complex) : PowerSeries Complex))).prod := by
       simpa only [Polynomial.coeToPowerSeries.ringHom_apply,
-        Multiset.map_map, Function.comp_apply] using
+        Multiset.map_map, Function.comp_def] using
         (map_multiset_prod Polynomial.coeToPowerSeries.ringHom
           (s.map (fun a ↦ 1 - Polynomial.C a * Polynomial.X)))
     _ = reciprocalRootProduct s := by
-      simp [reciprocalRootProduct, reciprocalRootFactor]
+      unfold reciprocalRootProduct
+      apply congrArg Multiset.prod
+      apply Multiset.map_congr rfl
+      intro a _
+      simp [reciprocalRootFactor]
 
 /-- The Artin coefficient polynomial satisfies the reciprocal-root
 logarithmic derivative identity. -/
@@ -199,7 +202,7 @@
     {K : Type*} [Field K] [Fintype K]
     (character : AddChar K Complex) (b : Fin 5 → K) {d : Nat}
     (hdpos : 0 < d) :
-    PowerSeries.X * PowerSeries.derivativeFun
+    PowerSeries.X * (PowerSeries.derivative Complex)
         (artinLPolynomial character b d : PowerSeries Complex) =
       (artinLPolynomial character b d : PowerSeries Complex) *
         reciprocalRootLogDerivative
diff --git a/Warning/Waring/Analytic/WeilRootRadius.lean b/Warning/Waring/Analytic/WeilRootRadius.lean
--- a/Warning/Waring/Analytic/WeilRootRadius.lean
+++ b/Warning/Waring/Analytic/WeilRootRadius.lean
@@ -28,7 +28,7 @@
     (hb : ∀ i : Fin 5, d < i.val + 1 → b i = 0) :
     ∀ z ∈ (artinLPolynomial ZMod.stdAddChar b d).reverse.roots,
       ‖z‖ ≤ Real.sqrt (p : Real) := by
-  letI : NeZero p := ⟨by omega⟩
+  let : NeZero p := ⟨by omega⟩
   have hcharacter :
       (ZMod.stdAddChar : AddChar (ZMod p) Complex) ≠ 1 := by
     intro htrivial
@@ -48,8 +48,8 @@
     have hmEq : h + 3 = m := by
       dsimp [h]
       omega
-    letI : NeZero (2 * m) := ⟨by omega⟩
-    letI : Fintype (FiniteField.Extension (ZMod p) p (2 * m)) :=
+    let : NeZero (2 * m) := ⟨by omega⟩
+    let : Fintype (FiniteField.Extension (ZMod p) p (2 * m)) :=
       Fintype.ofFinite _
     have hstep := Stepanov.norm_extension_pointPhase_sum_le
       hp hdpos hdp b hd5 hbtop hb h
diff --git a/Warning/Waring/Analytic/WeilTraceBridge.lean b/Warning/Waring/Analytic/WeilTraceBridge.lean
--- a/Warning/Waring/Analytic/WeilTraceBridge.lean
+++ b/Warning/Waring/Analytic/WeilTraceBridge.lean
@@ -30,8 +30,8 @@
           (IntermediateField.AdjoinSimple.gen K x)) := by
   classical
   let E := AlgebraicClosure L
-  letI : FiniteDimensional K K⟮x⟯ := IntermediateField.adjoin.finiteDimensional hx
-  letI : Algebra.IsSeparable K K⟮x⟯ :=
+  let : FiniteDimensional K K⟮x⟯ := IntermediateField.adjoin.finiteDimensional hx
+  let : Algebra.IsSeparable K K⟮x⟯ :=
     (IntermediateField.isSeparable_adjoin_simple_iff_isSeparable K L).2 hsep
   apply (algebraMap K E).injective
   rw [formalRootPhase_eq_sum_roots_map (algebraMap K E) b
diff --git a/Warning/Waring/Analytic/WeylCorrelation.lean b/Warning/Waring/Analytic/WeylCorrelation.lean
--- a/Warning/Waring/Analytic/WeylCorrelation.lean
+++ b/Warning/Waring/Analytic/WeylCorrelation.lean
@@ -41,6 +41,7 @@
       simp_rw [← Finset.mul_sum]
       simp only [← star_sum]
       simp only [star_mul, star_star]
+      simp only [starRingEnd_apply] at ih
       linear_combination ih
 
 /-- If every entry has norm one, the norm square is bounded by the diagonal
