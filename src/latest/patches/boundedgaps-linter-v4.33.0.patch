diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/CenteredCharacterReduction.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/CenteredCharacterReduction.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/CenteredCharacterReduction.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/CenteredCharacterReduction.lean
@@ -148,7 +148,7 @@
         ‖centeredTwistedChebyshevSum
           x χ.conductor χ.primitiveCharacter‖ := by
   have hq0 : q ≠ 0 := by omega
-  letI : NeZero q := ⟨hq0⟩
+  let : NeZero q := ⟨hq0⟩
   have hdiff := centeredPrimitive_sub_centered_eq (x := x) χ
   have hraw := norm_primitiveTwistedChebyshevSum_sub_le_log_mul_sq χ hx hq
   have hcenter :
@@ -194,7 +194,7 @@
             ‖centeredTwistedChebyshevSum
               x χ.conductor χ.primitiveCharacter‖ := by
   have hq0 : q ≠ 0 := by omega
-  letI : NeZero q := ⟨hq0⟩
+  let : NeZero q := ⟨hq0⟩
   have hpoint : ∀ χ : DirichletCharacter ℂ q,
       ‖centeredTwistedChebyshevSum x q χ‖ ≤
         (Real.log ((q * x : ℕ) : ℝ)) ^ 2 +
@@ -259,7 +259,7 @@
             ‖centeredTwistedChebyshevSum
               x χ.conductor χ.primitiveCharacter‖ := by
   have hq0 : q ≠ 0 := by omega
-  letI : NeZero q := ⟨hq0⟩
+  let : NeZero q := ⟨hq0⟩
   exact (abs_chebyshevProgressionSum_sub_global_le_centered_average ha).trans
     (inv_totient_mul_sum_norm_centered_le_log_sq_add_primitive hx hq)
 
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/CenteredPrimitiveMaxima.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/CenteredPrimitiveMaxima.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/CenteredPrimitiveMaxima.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/CenteredPrimitiveMaxima.lean
@@ -42,7 +42,7 @@
           ∑ χ : DirichletCharacter ℂ q,
             inducingPrimitiveCenteredEndpointMaximum x q χ := by
   have hq0 : q ≠ 0 := by omega
-  letI : NeZero q := ⟨hq0⟩
+  let : NeZero q := ⟨hq0⟩
   apply Finset.sup'_le (weightedEndpointRange_nonempty hx)
   intro y hy
   have hyBounds : 2 ≤ y ∧ y ≤ x := Finset.mem_Icc.mp hy
@@ -162,7 +162,7 @@
         (DirichletCharacter.changeLevel hd ψ.1).conductor
         (DirichletCharacter.changeLevel hd ψ.1).primitiveCharacter =
       centeredTwistedChebyshevSum y d ψ.1 := by
-  letI : NeZero q := ⟨by omega⟩
+  let : NeZero q := ⟨by omega⟩
   have hcon :
       (DirichletCharacter.changeLevel hd ψ.1).conductor = d := by
     rw [DirichletCharacter.conductor_changeLevel]
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletExplicitFormulaCandidateSingularities.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletExplicitFormulaCandidateSingularities.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletExplicitFormulaCandidateSingularities.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletExplicitFormulaCandidateSingularities.lean
@@ -67,7 +67,7 @@
     rw [Function.mem_support,
       MeromorphicOn.AnalyticOnNhd.divisor_apply hA hsU]
     lift analyticOrderAt f s to ℕ using htop with n hn
-    simp only [ENat.map_coe, WithTop.untop₀_coe]
+    simp only [ENat.map_natCast, WithTop.untop₀_coe]
     exact_mod_cast horder
   rw [dirichletExplicitFormulaCandidateSingularities]
   by_cases hchi : chi = 1
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletGoodHeightSelector.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletGoodHeightSelector.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletGoodHeightSelector.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletGoodHeightSelector.lean
@@ -67,7 +67,7 @@
   have hf_near (i : Fin (n + 1)) :
       |grid i - (f i : ℝ)| < radius :=
     Classical.choose_spec (hnear i)
-  letI := hS.fintype
+  let := hS.fintype
   have hcard : Fintype.card S < Fintype.card (Fin (n + 1)) := by
     simp only [Set.fintypeCard_eq_ncard, Fintype.card_fin]
     dsimp [n]
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletGoodHeightZeroCoverage.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletGoodHeightZeroCoverage.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletGoodHeightZeroCoverage.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletGoodHeightZeroCoverage.lean
@@ -140,7 +140,7 @@
   rw [Function.mem_support,
     MeromorphicOn.AnalyticOnNhd.divisor_apply hA hrhoU]
   lift analyticOrderAt riemannZeta₁ rho to ℕ using htop with n hn
-  simp only [ENat.map_coe, WithTop.untop₀_coe]
+  simp only [ENat.map_natCast, WithTop.untop₀_coe]
   have horder : analyticOrderAt riemannZeta₁ rho ≠ 0 :=
     (differentiable_riemannZeta₁.analyticAt rho
       |>.analyticOrderAt_ne_zero).mpr hzero
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletLFunctionClosedStripGrowth.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletLFunctionClosedStripGrowth.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletLFunctionClosedStripGrowth.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletLFunctionClosedStripGrowth.lean
@@ -132,7 +132,7 @@
   refine ⟨A + 2, by omega, ?_⟩
   intro q _ hq chi hchi s hslo hshi
   let d := chi.conductor
-  letI : NeZero d := ⟨chi.conductor_ne_zero⟩
+  let : NeZero d := ⟨chi.conductor_ne_zero⟩
   have hd1 : 1 < d := by
     have hdne : d ≠ 1 := by
       intro hd
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletLZeroDivisor.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletLZeroDivisor.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletLZeroDivisor.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletLZeroDivisor.lean
@@ -56,7 +56,7 @@
   rw [MeromorphicOn.AnalyticOnNhd.divisor_apply
     ((analyticOnNhd_LFunction_of_nontrivial hχ).mono (Set.subset_univ U)) hsU]
   have hfinite := analyticOrderAt_LFunction_ne_top hχ s
-  rw [← Nat.cast_analyticOrderNatAt hfinite, ENat.map_coe,
+  rw [← Nat.cast_analyticOrderNatAt hfinite, ENat.map_natCast,
     WithTop.untop₀_coe]
 
 /-- On a set containing `s`, divisor support is exactly ordinary vanishing. -/
@@ -71,7 +71,7 @@
   have htop := analyticOrderAt_LFunction_ne_top hχ s
   lift analyticOrderAt (DirichletCharacter.LFunction χ) s to ℕ using htop
     with n hn
-  simp only [ENat.map_coe, WithTop.untop₀_coe]
+  simp only [ENat.map_natCast, WithTop.untop₀_coe]
   constructor
   · intro hnInt
     have hnNat : n ≠ 0 := by exact_mod_cast hnInt
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletTwoSidedBadHeights.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletTwoSidedBadHeights.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletTwoSidedBadHeights.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/DirichletTwoSidedBadHeights.lean
@@ -227,7 +227,7 @@
           nlinarith [mul_le_mul hAZreal hlog hlogNonneg
             (by positivity : (0 : ℝ) ≤ A)]
     · let d := chi.conductor
-      letI : NeZero d := ⟨chi.conductor_ne_zero⟩
+      let : NeZero d := ⟨chi.conductor_ne_zero⟩
       have hdpos : 0 < d := Nat.pos_of_ne_zero (NeZero.ne d)
       have hdne : d ≠ 1 := by
         intro hd
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/FixedDiskLogDerivative.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/FixedDiskLogDerivative.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/FixedDiskLogDerivative.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/FixedDiskLogDerivative.lean
@@ -61,7 +61,7 @@
   have hfinite := analyticOrderAt_ne_top_on_closedBall (by positivity)
     hf_inner hc hz
   rw [selectedDivisor, MeromorphicOn.AnalyticOnNhd.divisor_apply hf_inner hz,
-    ← Nat.cast_analyticOrderNatAt hfinite, ENat.map_coe, WithTop.untop₀_coe]
+    ← Nat.cast_analyticOrderNatAt hfinite, ENat.map_natCast, WithTop.untop₀_coe]
 
 private theorem selectedDivisor_nonneg
     {f : ℂ -> ℂ} {c : ℂ} {R : ℝ}
@@ -109,7 +109,7 @@
   have hfinite := analyticOrderAt_ne_top_on_closedBall (by positivity) hf hc hz_outer
   rw [meromorphicOrderAt_selectedRawFactor (hf z hz_outer),
     selectedDivisor_apply_eq_order hR hf hc hz,
-    ← Nat.cast_analyticOrderNatAt hfinite, ENat.map_coe]
+    ← Nat.cast_analyticOrderNatAt hfinite, ENat.map_natCast]
   simp
 
 private theorem meromorphicOrderAt_selectedRawFactor_nonneg
@@ -122,7 +122,7 @@
   · have hfinite := analyticOrderAt_ne_top_on_closedBall (by positivity) hf hc hz
     rw [meromorphicOrderAt_selectedRawFactor (hf z hz),
       selectedDivisor, Function.locallyFinsuppWithin.apply_eq_zero_of_notMem _ hz_inner,
-      ← Nat.cast_analyticOrderNatAt hfinite, ENat.map_coe]
+      ← Nat.cast_analyticOrderNatAt hfinite, ENat.map_natCast]
     simp only [WithTop.coe_zero, neg_zero, zero_add]
     exact_mod_cast Nat.zero_le (analyticOrderNatAt f z)
 
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/GoldfeldCrossLevelCharacters.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/GoldfeldCrossLevelCharacters.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/GoldfeldCrossLevelCharacters.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/GoldfeldCrossLevelCharacters.lean
@@ -31,7 +31,7 @@
     {q : ℕ} [NeZero q]
     (chi1 chi : DirichletCharacter Complex q) :
     goldfeldCharactersDistinct chi1 chi ↔ chi1 ≠ chi := by
-  letI : NeZero (Nat.lcm q q) :=
+  let : NeZero (Nat.lcm q q) :=
     ⟨Nat.lcm_ne_zero (NeZero.ne q) (NeZero.ne q)⟩
   unfold goldfeldCharactersDistinct
   rw [show Nat.dvd_lcm_right q q = Nat.dvd_lcm_left q q from
@@ -49,7 +49,7 @@
     (hprimitive : DirichletCharacter.IsPrimitive chi)
     (hmodulus : q1 ≠ q) :
     goldfeldCharactersDistinct chi1 chi := by
-  letI : NeZero (Nat.lcm q1 q) :=
+  let : NeZero (Nat.lcm q1 q) :=
     ⟨Nat.lcm_ne_zero (NeZero.ne q1) (NeZero.ne q)⟩
   rw [goldfeldCharactersDistinct]
   intro heq
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/GoldfeldExceptionalCharacter.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/GoldfeldExceptionalCharacter.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/GoldfeldExceptionalCharacter.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/GoldfeldExceptionalCharacter.lean
@@ -67,7 +67,7 @@
         | mk q' hq' chi' hprimitive' hchi' hsquare' =>
           dsimp at hmodulus
           subst q'
-          letI : NeZero q :=
+          let : NeZero q :=
             ⟨Nat.ne_of_gt (Nat.zero_lt_of_lt hq)⟩
           rw [goldfeldCharactersDistinct_same_level_iff]
           intro hcharacters
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/GoldfeldFourFactorClosedStrip.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/GoldfeldFourFactorClosedStrip.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/GoldfeldFourFactorClosedStrip.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/GoldfeldFourFactorClosedStrip.lean
@@ -83,7 +83,7 @@
     simpa [B, T, hwim] using hL q hq chi hchi w hwlo hwhi
   let m := Nat.lcm q1 q
   have hmpos : 0 < m := Nat.lcm_pos (NeZero.pos q1) (NeZero.pos q)
-  letI : NeZero m := ⟨hmpos.ne'⟩
+  let : NeZero m := ⟨hmpos.ne'⟩
   have hqm : q ≤ m := Nat.le_of_dvd hmpos (Nat.dvd_lcm_right q1 q)
   have hm1 : 1 < m := hq.trans_le hqm
   have hmqq : m ≤ q1 * q :=
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/GoldfeldLeftLineBounds.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/GoldfeldLeftLineBounds.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/GoldfeldLeftLineBounds.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/GoldfeldLeftLineBounds.lean
@@ -106,7 +106,7 @@
     simpa [B, T, hwim] using hL q hq chi hchi w hwlo hwhi
   let m := Nat.lcm q1 q
   have hmpos : 0 < m := Nat.lcm_pos (NeZero.pos q1) (NeZero.pos q)
-  letI : NeZero m := ⟨hmpos.ne'⟩
+  let : NeZero m := ⟨hmpos.ne'⟩
   have hqm : q ≤ m := Nat.le_of_dvd hmpos (Nat.dvd_lcm_right q1 q)
   have hm1 : 1 < m := hq.trans_le hqm
   have hmqq : m ≤ q1 * q :=
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/GoldfeldQuantitativeResidue.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/GoldfeldQuantitativeResidue.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/GoldfeldQuantitativeResidue.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/GoldfeldQuantitativeResidue.lean
@@ -100,7 +100,7 @@
       _ ≤ 512 * delta * (q : ℝ) ^ (delta / 2) *
           (Real.log (q : ℝ)) ^ 2 := by
         gcongr
-  letI : NeZero (Nat.lcm q1 q) :=
+  let : NeZero (Nat.lcm q1 q) :=
     ⟨Nat.lcm_ne_zero (NeZero.ne q1) (NeZero.ne q)⟩
   have hlcmPos : 0 < Nat.lcm q1 q := NeZero.pos _
   have hq1lcm : q1 ≤ Nat.lcm q1 q :=
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/ImprimitivePolyaVinogradovPrefix.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/ImprimitivePolyaVinogradovPrefix.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/ImprimitivePolyaVinogradovPrefix.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/ImprimitivePolyaVinogradovPrefix.lean
@@ -269,7 +269,7 @@
     exact hchi (DirichletCharacter.eq_one_iff_conductor_eq_one.mpr hd)
   have hd : 1 < d := by omega
   have hdLeq : d ≤ q := Nat.le_of_dvd (NeZero.pos q) hdDvd
-  letI : NeZero d := ⟨hdPos.ne'⟩
+  let : NeZero d := ⟨hdPos.ne'⟩
   have hscalePos :
       0 < Real.sqrt (d : ℝ) * Real.log (d : ℝ) :=
     mul_pos (Real.sqrt_pos.2 (by exact_mod_cast hdPos))
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/InducingEulerProductZeroOrdinates.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/InducingEulerProductZeroOrdinates.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/InducingEulerProductZeroOrdinates.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/InducingEulerProductZeroOrdinates.lean
@@ -46,7 +46,7 @@
     MeromorphicOn.AnalyticOnNhd.divisor_apply hA hsU]
   lift analyticOrderAt (inducingEulerProduct chi) s to ℕ using htop
     with n hn
-  simp only [ENat.map_coe, WithTop.untop₀_coe]
+  simp only [ENat.map_natCast, WithTop.untop₀_coe]
   constructor
   · intro hnInt
     have hnNat : n ≠ 0 := by exact_mod_cast hnInt
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/PrimitiveCharacterAdditiveReduction.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/PrimitiveCharacterAdditiveReduction.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/PrimitiveCharacterAdditiveReduction.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/PrimitiveCharacterAdditiveReduction.lean
@@ -21,7 +21,7 @@
       χ⁻¹ (u : ZMod q) * ZMod.stdAddChar ((u : ZMod q) * n)) =
       ∑ a : ZMod q, χ⁻¹ a * ZMod.stdAddChar (a * n) := by
   classical
-  letI : Fintype (IsUnit.submonoid (ZMod q)) := Fintype.ofFinite _
+  let : Fintype (IsUnit.submonoid (ZMod q)) := Fintype.ofFinite _
   calc
     (∑ u : (ZMod q)ˣ,
         χ⁻¹ (u : ZMod q) * ZMod.stdAddChar ((u : ZMod q) * n)) =
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/PrimitiveConductorFibers.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/PrimitiveConductorFibers.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/PrimitiveConductorFibers.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/PrimitiveConductorFibers.lean
@@ -35,7 +35,7 @@
 theorem card_primitiveCharacters_le_totient
     {q : ℕ} (hq : 0 < q) :
     Fintype.card (primitiveCharacters q) ≤ q.totient := by
-  letI : NeZero q := ⟨hq.ne'⟩
+  let : NeZero q := ⟨hq.ne'⟩
   calc
     Fintype.card (primitiveCharacters q) ≤
         Fintype.card (DirichletCharacter ℂ q) :=
@@ -74,7 +74,7 @@
     (∑ χ : charactersOfConductor q d, F χ.1) =
       ∑ ψ : primitiveCharacters d,
         F (DirichletCharacter.changeLevel hd ψ.1) := by
-  letI : NeZero q := ⟨by omega⟩
+  let : NeZero q := ⟨by omega⟩
   apply Fintype.sum_equiv (primitiveCharactersEquivConductorFiber hq hd).symm
       (fun χ : charactersOfConductor q d ↦ F χ.1)
       (fun ψ : primitiveCharacters d ↦
@@ -150,7 +150,7 @@
     {q : ℕ} (hq : 0 < q) :
     q.totient =
       ∑ d : q.divisors, Fintype.card (primitiveCharacters d.1) := by
-  letI : NeZero q := ⟨by omega⟩
+  let : NeZero q := ⟨by omega⟩
   calc
     q.totient = Nat.card (DirichletCharacter ℂ q) :=
       (DirichletCharacter.card_eq_totient_of_hasEnoughRootsOfUnity ℂ q).symm
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/QuadraticZetaSwappedEulerRemainder.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/QuadraticZetaSwappedEulerRemainder.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/QuadraticZetaSwappedEulerRemainder.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/QuadraticZetaSwappedEulerRemainder.lean
@@ -197,7 +197,7 @@
     Measurable (quadraticEulerCutoff X) := by
   unfold quadraticEulerCutoff
   have hzero : MeasurableSet ({(0 : ℝ)} : Set ℝ) := measurableSet_singleton (0 : ℝ)
-  apply Measurable.ite (by simpa only [Set.setOf_eq_eq_singleton] using hzero)
+  apply Measurable.ite (by simpa only [Set.ofPred_eq_eq_singleton] using hzero)
     measurable_const
   have hdiv : Measurable (fun t : ℝ ↦ (X : ℝ) / t) :=
     measurable_const.div measurable_id
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/RawPrimitiveMaxima.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/RawPrimitiveMaxima.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/RawPrimitiveMaxima.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/RawPrimitiveMaxima.lean
@@ -23,7 +23,7 @@
 theorem primitiveCharacter_ne_one_of_one_lt
     {d : ℕ} (hd : 1 < d) (ψ : primitiveCharacters d) :
     ψ.1 ≠ (1 : DirichletCharacter ℂ d) := by
-  letI : NeZero d := ⟨by omega⟩
+  let : NeZero d := ⟨by omega⟩
   intro hψ
   have hconductorOne : ψ.1.conductor = 1 :=
     (DirichletCharacter.eq_one_iff_conductor_eq_one).mp hψ
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/RiemannZetaFixedDisk.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/RiemannZetaFixedDisk.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/RiemannZetaFixedDisk.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/RiemannZetaFixedDisk.lean
@@ -42,7 +42,7 @@
   rw [MeromorphicOn.AnalyticOnNhd.divisor_apply
     (analyticOnNhd_riemannZeta₁_fixedDisk.mono (Set.subset_univ U)) hsU]
   have hfinite := analyticOrderAt_riemannZeta₁_ne_top_fixedDisk s
-  rw [← Nat.cast_analyticOrderNatAt hfinite, ENat.map_coe,
+  rw [← Nat.cast_analyticOrderNatAt hfinite, ENat.map_natCast,
     WithTop.untop₀_coe]
 
 /-- Regularized-zeta divisor support is exactly regularized-zeta vanishing. -/
@@ -55,7 +55,7 @@
       (analyticOnNhd_riemannZeta₁_fixedDisk.mono (Set.subset_univ U)) hsU]
   have htop := analyticOrderAt_riemannZeta₁_ne_top_fixedDisk s
   lift analyticOrderAt riemannZeta₁ s to ℕ using htop with n hn
-  simp only [ENat.map_coe, WithTop.untop₀_coe]
+  simp only [ENat.map_natCast, WithTop.untop₀_coe]
   constructor
   · intro hnInt
     have hnNat : n ≠ 0 := by exact_mod_cast hnInt
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/RiemannZetaHeightPole.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/RiemannZetaHeightPole.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/RiemannZetaHeightPole.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/RiemannZetaHeightPole.lean
@@ -49,7 +49,7 @@
         (Set.subset_univ (closedBall ((2 : ℂ) + t * I) 2)))
       (mem_closedBall.mpr hrho)]
     have hfinite := analyticOrderAt_riemannZeta₁_ne_top rho
-    rw [← Nat.cast_analyticOrderNatAt hfinite, ENat.map_coe,
+    rw [← Nat.cast_analyticOrderNatAt hfinite, ENat.map_natCast,
       WithTop.untop₀_coe]
   · rw [if_neg hrho,
       Function.locallyFinsuppWithin.apply_eq_zero_of_notMem _
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/SiegelWalfiszEndpointMaximum.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/SiegelWalfiszEndpointMaximum.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/SiegelWalfiszEndpointMaximum.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/SiegelWalfiszEndpointMaximum.lean
@@ -182,7 +182,7 @@
       hdLog.trans (by simpa [L, l] using hpower)
     have hXsLtY : Xs < y := by
       exact_mod_cast hXsRoot.trans_lt hyLarge
-    letI : NeZero d := ⟨by omega⟩
+    let : NeZero d := ⟨by omega⟩
     have hpointY := hpoint y (by omega) d ψ.1
       (primitiveCharacter_ne_one_of_one_lt hd ψ) hdLogY
     have hquarter : L / 4 ≤ l := by linarith
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/SiegelZeroFreeRegion.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/SiegelZeroFreeRegion.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/SiegelZeroFreeRegion.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/SiegelZeroFreeRegion.lean
@@ -131,7 +131,7 @@
       exact hchi
         (DirichletCharacter.eq_one_iff_conductor_eq_one.mpr hconductor)
     omega
-  letI : NeZero chi.conductor := ⟨chi.conductor_ne_zero⟩
+  let : NeZero chi.conductor := ⟨chi.conductor_ne_zero⟩
   have hprimitiveNeOne : chi.primitiveCharacter ≠ 1 := by
     intro hprincipal
     apply hchi
diff --git a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/VaughanSecondTermPolyaVinogradov.lean b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/VaughanSecondTermPolyaVinogradov.lean
--- a/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/VaughanSecondTermPolyaVinogradov.lean
+++ b/BoundedGaps/BoundedGaps/BombieriVinogradov/Analytic/VaughanSecondTermPolyaVinogradov.lean
@@ -20,7 +20,7 @@
     (a b : ℕ) :
     ‖dirichletCharacterIntervalSum a b q chi‖ <
       Real.sqrt (q : ℝ) * Real.log (q : ℝ) := by
-  letI : NeZero q := ⟨Nat.ne_zero_of_lt hq⟩
+  let : NeZero q := ⟨Nat.ne_zero_of_lt hq⟩
   have hboundPos :
       0 < Real.sqrt (q : ℝ) * Real.log (q : ℝ) := by
     exact mul_pos (Real.sqrt_pos.2 (by exact_mod_cast Nat.zero_lt_of_lt hq))
@@ -74,7 +74,7 @@
     ‖vaughanTwistedSumTwo V y q chi‖ <
       Real.sqrt (q : ℝ) * V *
         (Real.log ((x : ℝ) * V)) ^ 2 := by
-  letI : NeZero q := ⟨Nat.ne_zero_of_lt hq⟩
+  let : NeZero q := ⟨Nat.ne_zero_of_lt hq⟩
   have hqpos : (0 : ℝ) < q := by
     exact_mod_cast Nat.zero_lt_of_lt hq
   have hsqrtq : 0 < Real.sqrt (q : ℝ) := Real.sqrt_pos.2 hqpos
diff --git a/BoundedGaps/BoundedGaps/Maynard/ConcreteSimplexOuter.lean b/BoundedGaps/BoundedGaps/Maynard/ConcreteSimplexOuter.lean
--- a/BoundedGaps/BoundedGaps/Maynard/ConcreteSimplexOuter.lean
+++ b/BoundedGaps/BoundedGaps/Maynard/ConcreteSimplexOuter.lean
@@ -87,7 +87,7 @@
     by_contra hcard
     have hzero : Fintype.card H = 0 := Nat.eq_zero_of_not_pos hcard
     have hEmpty : IsEmpty H := Fintype.card_eq_zero_iff.mp hzero
-    letI := hEmpty
+    let := hEmpty
     have houter := (Finset.mem_filter.mp hj).2
     simp at houter
     linarith
@@ -238,7 +238,7 @@
       simp only [Finset.sum_const, Finset.card_univ, nsmul_eq_mul]
       rw [hmul]
       linarith
-    letI : Nonempty H := Fintype.card_pos_iff.mp (hcard j hj)
+    let : Nonempty H := Fintype.card_pos_iff.mp (hcard j hj)
     apply lt_trans hmargin
     apply Finset.sum_lt_sum_of_nonempty Finset.univ_nonempty
     intro h hh
diff --git a/BoundedGaps/BoundedGaps/Maynard/FaceDecomposition.lean b/BoundedGaps/BoundedGaps/Maynard/FaceDecomposition.lean
--- a/BoundedGaps/BoundedGaps/Maynard/FaceDecomposition.lean
+++ b/BoundedGaps/BoundedGaps/Maynard/FaceDecomposition.lean
@@ -80,7 +80,7 @@
 theorem faceCoordinateEquiv_measurePreserving {n : ℕ} (m : Fin (n + 1)) :
     MeasurePreserving (faceCoordinateEquiv m) volume volume := by
   let p : Fin (n + 1) → Prop := fun i => i = m
-  letI : Fintype {i : Fin (n + 1) // p i} := Subtype.fintype p
+  let : Fintype {i : Fin (n + 1) // p i} := Subtype.fintype p
   let e := MeasurableEquiv.piEquivPiSubtypeProd
     (fun _ : Fin (n + 1) => ℝ) p
   let e1 := MeasurableEquiv.piUnique
@@ -282,7 +282,7 @@
       u ∈ maynardSimplex n := by
   rw [maynardSimplex_eq_radius]
   unfold maynardFaceSimplex maynardSimplexRadius
-  simp only [Set.mem_setOf_eq, faceReindexEquiv_sum]
+  simp only [Set.mem_ofPred_eq, faceReindexEquiv_sum]
   constructor
   · rintro ⟨hu, hsum⟩
     exact ⟨fun i => by
diff --git a/BoundedGaps/BoundedGaps/Maynard/ImprovedGPY/S2TauFiber.lean b/BoundedGaps/BoundedGaps/Maynard/ImprovedGPY/S2TauFiber.lean
--- a/BoundedGaps/BoundedGaps/Maynard/ImprovedGPY/S2TauFiber.lean
+++ b/BoundedGaps/BoundedGaps/Maynard/ImprovedGPY/S2TauFiber.lean
@@ -190,7 +190,7 @@
     _ ≤ (3 * Fintype.card H) ^ ω m * Fintype.card H := by
       apply Nat.mul_le_mul_right
       apply Nat.pow_le_pow_right
-      · haveI : Nonempty H := hH.to_subtype
+      · have : Nonempty H := hH.to_subtype
         have hcard : 0 < Fintype.card H := Fintype.card_pos
         omega
       · exact homega
diff --git a/BoundedGaps/BoundedGaps/Maynard/MaynardLambdaSharpBound.lean b/BoundedGaps/BoundedGaps/Maynard/MaynardLambdaSharpBound.lean
--- a/BoundedGaps/BoundedGaps/Maynard/MaynardLambdaSharpBound.lean
+++ b/BoundedGaps/BoundedGaps/Maynard/MaynardLambdaSharpBound.lean
@@ -26,7 +26,7 @@
   let T := (lambdaQuotientTupleSupport H R d).image (divisorTupleProduct H)
   let P := lambdaDivisorQuotientProductSupport H R d
   have hk : 1 ≤ k := by
-    letI : Nonempty H := hH.to_subtype
+    let : Nonempty H := hH.to_subtype
     exact Fintype.card_pos
   calc
     lambdaQuotientWeightSum H R d ≤
@@ -79,7 +79,7 @@
     |maynardCoefficient H R W F d| ≤
       B * (1 + Real.log R) ^ (2 * (Fintype.card H) ^ 2) := by
   have hk : 1 ≤ Fintype.card H := by
-    letI : Nonempty H := hH.to_subtype
+    let : Nonempty H := hH.to_subtype
     exact Fintype.card_pos
   calc
     |maynardCoefficient H R W F d| ≤
diff --git a/BoundedGaps/BoundedGaps/Maynard/MaynardS2OuterCorrectionMean.lean b/BoundedGaps/BoundedGaps/Maynard/MaynardS2OuterCorrectionMean.lean
--- a/BoundedGaps/BoundedGaps/Maynard/MaynardS2OuterCorrectionMean.lean
+++ b/BoundedGaps/BoundedGaps/Maynard/MaynardS2OuterCorrectionMean.lean
@@ -144,7 +144,7 @@
             simp [hc0]
           · have hdQ : Q ≤ (d : ℕ) := by
               have hnot := d.property
-              simp only [Sc, S, Set.mem_compl_iff, Set.mem_setOf_eq,
+              simp only [Sc, S, Set.mem_compl_iff, Set.mem_ofPred_eq,
                 Finset.mem_Ioc, not_and] at hnot
               omega
             have hlog : Real.log Q ≤ Real.log (d : ℕ) := by
diff --git a/BoundedGaps/BoundedGaps/Maynard/MaynardS2ReciprocalGCorrectionMean.lean b/BoundedGaps/BoundedGaps/Maynard/MaynardS2ReciprocalGCorrectionMean.lean
--- a/BoundedGaps/BoundedGaps/Maynard/MaynardS2ReciprocalGCorrectionMean.lean
+++ b/BoundedGaps/BoundedGaps/Maynard/MaynardS2ReciprocalGCorrectionMean.lean
@@ -150,7 +150,7 @@
             simp [hc0]
           · have hdQ : Q ≤ (d : ℕ) := by
               have hnot := d.property
-              simp only [Sc, S, Set.mem_compl_iff, Set.mem_setOf_eq,
+              simp only [Sc, S, Set.mem_compl_iff, Set.mem_ofPred_eq,
                 Finset.mem_Ioc, not_and] at hnot
               omega
             have hlog : Real.log Q ≤ Real.log (d : ℕ) := by
diff --git a/BoundedGaps/BoundedGaps/Maynard/PrimeMertens.lean b/BoundedGaps/BoundedGaps/Maynard/PrimeMertens.lean
--- a/BoundedGaps/BoundedGaps/Maynard/PrimeMertens.lean
+++ b/BoundedGaps/BoundedGaps/Maynard/PrimeMertens.lean
@@ -233,7 +233,7 @@
 private theorem summable_nonprimeMangoldtTerm :
     Summable fun n : ℕ =>
       (if n.Prime then 0 else ArithmeticFunction.vonMangoldt n) / (n : ℝ) := by
-  letI : Subsingleton (ZMod 1) := ZMod.subsingleton_iff.mpr rfl
+  let : Subsingleton (ZMod 1) := ZMod.subsingleton_iff.mpr rfl
   have hcast : ∀ n : ℕ, (n : ZMod 1) = 0 :=
     fun n => Subsingleton.elim _ _
   simpa [ArithmeticFunction.vonMangoldt.residueClass, hcast] using
diff --git a/BoundedGaps/BoundedGaps/Maynard/ReciprocalTotientCorrectionEndpoint.lean b/BoundedGaps/BoundedGaps/Maynard/ReciprocalTotientCorrectionEndpoint.lean
--- a/BoundedGaps/BoundedGaps/Maynard/ReciprocalTotientCorrectionEndpoint.lean
+++ b/BoundedGaps/BoundedGaps/Maynard/ReciprocalTotientCorrectionEndpoint.lean
@@ -294,7 +294,7 @@
             simp [hc0]
           · have hdQ : Q ≤ (d : ℕ) := by
               have hnot := d.property
-              simp only [Sc, S, Set.mem_compl_iff, Set.mem_setOf_eq,
+              simp only [Sc, S, Set.mem_compl_iff, Set.mem_ofPred_eq,
                 Finset.mem_Ioc, not_and] at hnot
               omega
             have hlog : Real.log Q ≤ Real.log (d : ℕ) := by
diff --git a/BoundedGaps/BoundedGaps/Maynard/SimplexMoments.lean b/BoundedGaps/BoundedGaps/Maynard/SimplexMoments.lean
--- a/BoundedGaps/BoundedGaps/Maynard/SimplexMoments.lean
+++ b/BoundedGaps/BoundedGaps/Maynard/SimplexMoments.lean
@@ -80,7 +80,7 @@
   have he := volume_preserving_piUnique (fun _ : Fin 1 => ℝ)
   have hmem (t : Fin 1 → ℝ) :
       t ∈ maynardSimplex 1 ↔ e t ∈ Set.Icc (0 : ℝ) 1 := by
-    simp only [maynardSimplex, maynardCube, maynardCubeOf, Set.mem_setOf_eq,
+    simp only [maynardSimplex, maynardCube, maynardCubeOf, Set.mem_ofPred_eq,
       Fin.sum_univ_succ]
     constructor
     · intro ht
@@ -287,7 +287,7 @@
         maynardSimplexRadius (n + 1) r ↔
       0 ≤ x ∧ t ∈ maynardSimplexRadius n (r - x) := by
   unfold maynardSimplexRadius
-  simp only [Set.mem_setOf_eq]
+  simp only [Set.mem_ofPred_eq]
   simp [MeasurableEquiv.piFinSuccAbove_symm_apply, Fin.insertNthEquiv,
     Fin.forall_fin_succ, Fin.sum_univ_succ]
   constructor
@@ -311,7 +311,7 @@
 theorem simplexRadius_eq_empty_of_neg (k : ℕ) {r : ℝ} (hr : r < 0) :
     maynardSimplexRadius k r = ∅ := by
   ext t
-  simp only [maynardSimplexRadius, Set.mem_setOf_eq, Set.mem_empty_iff_false,
+  simp only [maynardSimplexRadius, Set.mem_ofPred_eq, Set.mem_empty_iff_false,
     iff_false]
   intro ht
   have hsum : 0 ≤ ∑ i, t i := Finset.sum_nonneg fun i hi => ht.1 i
@@ -454,7 +454,7 @@
     simp only [Fin.prod_univ_succ, Fin.sum_univ_succ]
     field_simp
     push_cast
-    ring
+    ring_nf
 
 theorem maynardSimplex_monomial_integral (k : ℕ) (a : Fin k → ℕ) (b : ℕ) :
     (∫ t in maynardSimplex k,
diff --git a/BoundedGaps/BoundedGaps/Proof/LiminfBridge.lean b/BoundedGaps/BoundedGaps/Proof/LiminfBridge.lean
--- a/BoundedGaps/BoundedGaps/Proof/LiminfBridge.lean
+++ b/BoundedGaps/BoundedGaps/Proof/LiminfBridge.lean
@@ -49,7 +49,7 @@
 theorem nthPrime_isConsecutivePrimePair (n : ℕ) :
     IsConsecutivePrimePair (Nat.nth Nat.Prime n)
       (Nat.nth Nat.Prime (n + 1)) := by
-  have hmono := Nat.nth_strictMono Nat.infinite_setOf_prime
+  have hmono := Nat.nth_strictMono Nat.infinite_setOfPred_prime
   refine ⟨Nat.prime_nth_prime n, Nat.prime_nth_prime (n + 1),
     hmono (Nat.lt_succ_self n), ?_⟩
   intro r hleft hright hr
@@ -65,7 +65,7 @@
   have hNprimeN : N < Nat.nth Nat.Prime N :=
     (show N < N + 2 by omega).trans_le (Nat.add_two_le_nth_prime N)
   have hprimeNn : Nat.nth Nat.Prime N < Nat.nth Nat.Prime n :=
-    (Nat.nth_strictMono Nat.infinite_setOf_prime) hNn
+    (Nat.nth_strictMono Nat.infinite_setOfPred_prime) hNn
   exact ⟨Nat.nth Nat.Prime n, Nat.nth Nat.Prime (n + 1),
     hNprimeN.trans hprimeNn, nthPrime_isConsecutivePrimePair n, hgap⟩
 
@@ -77,26 +77,26 @@
     h (Nat.nth Nat.Prime (N + 1))
   obtain ⟨hpp, hqp, hpq, hbetween⟩ := hpair
   have hp_range : p ∈ Set.range (Nat.nth Nat.Prime) := by
-    rw [Nat.range_nth_of_infinite Nat.infinite_setOf_prime]
+    rw [Nat.range_nth_of_infinite Nat.infinite_setOfPred_prime]
     exact hpp
   obtain ⟨m, rfl⟩ := hp_range
   have hq_range : q ∈ Set.range (Nat.nth Nat.Prime) := by
-    rw [Nat.range_nth_of_infinite Nat.infinite_setOf_prime]
+    rw [Nat.range_nth_of_infinite Nat.infinite_setOfPred_prime]
     exact hqp
   obtain ⟨j, rfl⟩ := hq_range
   have hNm : N < m := by
     have : N + 1 < m :=
-      (Nat.nth_lt_nth Nat.infinite_setOf_prime).mp hp_large
+      (Nat.nth_lt_nth Nat.infinite_setOfPred_prime).mp hp_large
     omega
   have hmj : m < j :=
-    (Nat.nth_lt_nth Nat.infinite_setOf_prime).mp hpq
+    (Nat.nth_lt_nth Nat.infinite_setOfPred_prime).mp hpq
   have hj : j = m + 1 := by
     apply Nat.le_antisymm
     · apply Nat.le_of_not_gt
       intro hsuccj
       exact hbetween (Nat.nth Nat.Prime (m + 1))
-        ((Nat.nth_strictMono Nat.infinite_setOf_prime) (Nat.lt_succ_self m))
-        ((Nat.nth_strictMono Nat.infinite_setOf_prime) hsuccj)
+        ((Nat.nth_strictMono Nat.infinite_setOfPred_prime) (Nat.lt_succ_self m))
+        ((Nat.nth_strictMono Nat.infinite_setOfPred_prime) hsuccj)
         (Nat.prime_nth_prime (m + 1))
     · omega
   subst j
